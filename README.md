**description**

super boilerplate react-flux app using `fluxxed_up` and `hot-loading` enabled in dev.

have fun!

**setup**

`npm install`

**startup**

`npm run dev`

**see it!**

`http://localhost:3001/#/`